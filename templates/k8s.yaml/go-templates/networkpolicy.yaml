piVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{name}}
spec:
  podSelector:
    matchLabels:
      app: {{name}}
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: ingress-nginx
